import { isometricSvgElem, isometricSvgFreeForm } from './index';

// класс просто для проверки передачи guid в КО (Марии Пискунова)
export class IsometricActiveElement {
  selectElementByGuid(guid) {
    console.log([guid]);
  }
}
